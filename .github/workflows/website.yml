name: Documentation

on:
  push:
    branches:
      - main
  # Allow running manually
  workflow_dispatch:

# Allow one concurrent deployment
concurrency:
  group: docs-website
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  prepare:
    name: Version info
    runs-on: ubuntu-slim

    outputs:
      matrix: ${{ steps.matrix.outputs.result }}
      versions: ${{ steps.versions.outputs.result }}

    steps:
      - name: Checkout version-info
        uses: actions/checkout@v6
        with:
          sparse-checkout: version-info.toml
          sparse-checkout-cone-mode: false

      - name: Produce build matrix
        id: matrix
        run: |
          {
            echo -n "result="
            yq --no-colors --output-format=json --indent=0 '
              [
                .versions
                | to_entries
                | reverse
                | .[]
                | {
                    "ref": .value.branch,
                    "name": .key,
                    "base-href": "/nixvim/"
                  }
                | select(.ref != "main").sub-path = .name
                | select(.ref != "main").base-href = "\(.base-href)\(.name)/"
              ]
            ' version-info.toml
          } >> "$GITHUB_OUTPUT"

      - name: Produce "other versions" array
        id: versions
        run: |
          {
            echo -n "result="
            yq --no-colors --output-format=json --indent=0 '
              [
                .versions
                | to_entries
                | reverse
                | .[]
                | {
                    "branch": .value.branch,
                    "nixpkgsBranch": .value.channel,
                    "baseHref": "/nixvim/\(.key)/",
                    "status": .value.status
                  }
                | select(.branch == "main").baseHref = "/nixvim/"
              ]
            ' version-info.toml
          } >> "$GITHUB_OUTPUT"

  build:
    name: Build ${{ matrix.name }}
    runs-on: ubuntu-latest
    needs: prepare

    strategy:
      fail-fast: true
      matrix:
        include: ${{ fromJSON(needs.prepare.outputs.matrix) }}

    steps:
      - name: Install nix
        uses: cachix/install-nix-action@v31

      - name: Configure cachix
        uses: cachix/cachix-action@v17
        with:
          name: nix-community
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      # --apply can be used with `nix eval`, but there's no way to build the result.
      # Therefore, we build a downstream flake taking the Nixvim branch as its input
      - name: Setup flake
        env:
          ref: ${{ matrix.ref }}
        run: |
          cat > flake.nix <<EOF
          {
            inputs.nixvim.url = "";
            outputs = inputs: {
              packages.x86_64-linux.default = inputs.nixvim.packages.x86_64-linux.docs.override {
                baseHref = builtins.getEnv "baseHref";
                availableVersions = builtins.fromJSON (builtins.getEnv "availableVersions");
              };
            };
          }
          EOF

          git init --initial-branch=main
          git add flake.nix
          nix flake lock --override-input nixvim "github:$GITHUB_REPOSITORY/$ref"
          git add flake.lock

      # Build the `docs` flake output from the corresponding branch
      # Uses --impure to allow `getEnv` in `.override`
      - name: Build docs
        env:
          outLink: build/${{ matrix.sub-path }}
          baseHref: ${{ matrix.base-href }}
          availableVersions: ${{ needs.prepare.outputs.versions }}
        run: |
          nix build --impure --out-link "$outLink"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: build
          name: ${{ matrix.name }}-docs
          retention-days: 1

  combine:
    name: Combine builds
    runs-on: ubuntu-24.04-arm
    needs: build

    env:
      in: artifacts
      out: combined

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          path: ${{ env.in }}
          pattern: "*-docs"
          merge-multiple: false

      - name: Extract archives
        run: |
          mkdir -p "$out"
          find "$in" -name artifact.tar \
          | parallel tar \
              --verbose \
              --extract \
              --file {} \
              --directory "$out"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ${{ env.out }}

  deploy:
    name: Deploy
    runs-on: ubuntu-24.04-arm
    needs: combine

    permissions:
      pages: write # to deploy to Pages
      id-token: write # to verify the deployment originates from an appropriate source

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
